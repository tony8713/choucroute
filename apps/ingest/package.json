{
  "name": "@choucroute/ingest",
  "version": "0.1.0",
  "private": true,
  "description": "earbox audio-ingest: a small Bun WebSocket server that accepts a live PCM S16LE 16k mono stream from the box, tracks stats, and exposes a clean seam where cloud STT attaches. Deployed on Fly.io (region sin).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build src/server.ts --target=bun --outdir=dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "stream-file": "bun scripts/stream-file.ts"
  },
  "devDependencies": {
    "@choucroute/config": "workspace:*",
    "@types/bun": "^1.3.13"
  }
}
